
1. ./bootstrap && ./configure && make

2. install following cpan modules:

    cpan install Test::More
    cpan install Test::Base
    cpan install Cache::Memcached::Fast

3. prove -v t/*.t

see each test cases for details.

